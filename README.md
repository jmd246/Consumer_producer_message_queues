# Consumer_producer_message_queues
A distributed system with two independent services running on different ports, using a message broker to implement point-to-point messaging queues for asynchronous communication between a producer and a consumer.
